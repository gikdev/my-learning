# https://taskfile.dev
version: '3'

vars:
  # GREETING: Hello, World!

dotenv:
  - .env

tasks:
  default: task --list-all
  restore: dotnet restore ./Moduben.slnx
  run: dotnet run --project ./src/Moduben.Api/
