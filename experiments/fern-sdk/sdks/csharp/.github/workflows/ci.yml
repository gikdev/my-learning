name: ci

on: [push]

env:
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  DOTNET_NOLOGO: true

jobs:
  compile:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - uses: actions/checkout@master

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.x

      - name: Print .NET info
        run: dotnet --info

      - name: Install tools
        run: dotnet tool restore

      - name: Restore dependencies
        run: dotnet restore src/Bahrami85Api/Bahrami85Api.csproj

      - name: Build Release
        run: dotnet build src/Bahrami85Api/Bahrami85Api.csproj -c Release --no-restore

  unit-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.x

      - name: Print .NET info
        run: dotnet --info

      - name: Install tools
        run: |
          dotnet tool restore

      - name: Restore dependencies
        run: dotnet restore src/Bahrami85Api.Test/Bahrami85Api.Test.csproj

      - name: Build Release
        run: dotnet build src/Bahrami85Api.Test/Bahrami85Api.Test.csproj -c Release --no-restore

      - name: Run Tests
        run: dotnet test src/Bahrami85Api.Test/Bahrami85Api.Test.csproj -c Release --no-build --no-restore


